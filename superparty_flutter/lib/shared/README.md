# Shared

Componente reutilizabile între feature-uri.

## Structură

- `widgets/` - Widget-uri reutilizabile (error states, loading, etc.)
- `theme/` - Theme configuration (colors, text styles, etc.)
